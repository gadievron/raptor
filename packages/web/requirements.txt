# Requirements specific to the `packages/web` module.
# Exact pins per the project's "no loose deps" policy.
beautifulsoup4==4.14.3
playwright==1.58.0
requests==2.33.0
pyyaml==6.0.3
cryptography==46.0.3
