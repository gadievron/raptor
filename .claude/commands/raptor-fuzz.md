---
description: Binary fuzzing with AFL++ integration (alias for /fuzz)
dispatch: skill
exclude_from_listing: true
---

# /raptor-fuzz - alias for /fuzz

This command is retained as a back-compat alias. The canonical
slash-command is `/fuzz` — please use that going forward.

See: fuzz.md
