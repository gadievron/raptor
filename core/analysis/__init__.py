"""Security analysis modules — reasoning about code properties.

Taint approximation, reachability analysis, binary oracle verdicts,
dataflow, CFG construction, and sanitizer identification.  Distinct
from ``core/inventory/`` which captures structural facts (what's in
the codebase — files, functions, call sites, coverage).
"""
