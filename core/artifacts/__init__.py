"""Shared helpers for RAPTOR's persisted analysis artifacts.

Currently hosts the provenance stamp (see ``core.artifacts.provenance``)
that marks LLM-derived artifact content as untrusted per docs/security.md
invariant I2-(b).
"""
