import java.security.MessageDigest;

public class weakhash_java_pos {
    public void hashers() throws Exception {
        MessageDigest a = MessageDigest.getInstance("MD2");
        MessageDigest b = MessageDigest.getInstance("MD4");
        MessageDigest c = MessageDigest.getInstance("MD5");
        MessageDigest d = MessageDigest.getInstance("SHA1");
        MessageDigest e = MessageDigest.getInstance("SHA-1");
    }
}
